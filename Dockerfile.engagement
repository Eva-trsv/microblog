FROM golang:1.25-alpine AS build

WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o /out/engagement ./services/engagement

FROM alpine:3.22

WORKDIR /app
COPY --from=build /out/engagement /app/engagement
EXPOSE 8081
ENTRYPOINT ["/app/engagement"]
